import { SexofilterdataPipe } from './sexofilterdata.pipe';

describe('SexofilterdataPipe', () => {
  it('create an instance', () => {
    const pipe = new SexofilterdataPipe();
    expect(pipe).toBeTruthy();
  });
});
